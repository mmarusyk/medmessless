<header class="bg-white shadow-md py-1">
    <nav class="px-4 lg:px-6">
        <div class="flex justify-between items-center">
            <%= link_to root_path, class: "flex items-center" do %>
              <span class="text-2xl ml-2 font-bold text-darkblue">MedMessless</span>
            <% end %>
            <div>
              <%= greeting(current_user) %>
            </div>
            <div>
              <%= link_to 'Logout',
                destroy_user_session_path,
                data: { turbo_method: :delete },
                class: "px-4 py-1 text-xl black-link"
              %>
            </div>
        </div>
    </nav>
</header>
